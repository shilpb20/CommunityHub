@model CommunityHub.Core.Dtos.RegistrationRequestDto

<div class="col-4">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th colspan="2" class="text-center" style="border-top: 2px solid #dee2e6; border-bottom: 2px solid #dee2e6;">
                    Spouse Details
                </th>
            </tr>
        </thead>
        <tbody>
            @if (Model.RegistrationInfo.SpouseInfo == null)
            {
                <tr>
                    <td colspan="2" class="text-center">N/A</td>
                </tr>
            }
            else
            {
                @if (!string.IsNullOrEmpty(Model.RegistrationInfo.SpouseInfo.Email))
                {
                <tr>
                    <td><strong>Email:</strong></td>
                        <td>@Model.RegistrationInfo.SpouseInfo.Email</td>
                </tr>
                }
                @if (!string.IsNullOrEmpty(Model.RegistrationInfo.SpouseInfo.ContactNumber))
                {
                    <tr>
                        <td><strong>Contact:</strong></td>
                        <td>@Model.RegistrationInfo.SpouseInfo.CountryCode - @Model.RegistrationInfo.SpouseInfo.ContactNumber</td>
                    </tr>
                }
                @if (!string.IsNullOrEmpty(Model.RegistrationInfo.SpouseInfo.HomeTown))
                {
                    <tr>
                        <td><strong>Home Town:</strong></td>
                        <td>@Model.RegistrationInfo.SpouseInfo.HomeTown</td>
                    </tr>
                }
                @if (!string.IsNullOrEmpty(Model.RegistrationInfo.SpouseInfo.HouseName))
                {
                    <tr>
                        <td><strong>House Name:</strong></td>
                        <td>@Model.RegistrationInfo.SpouseInfo.HouseName</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>